import Route from '@ember/routing/route';

export default Route.extend({

	// you could also set the model here / or change the model data etc. - but for now, it's inheriting the model from it's parent

});
